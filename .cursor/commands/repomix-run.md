---
description: Generates a codebase snapshot quietly
---

# Your Context Action Prompt

Please execute this exact command in the terminal to cleanly compile the new snapshot:

```bash
npx repomix && echo "✅ Repomix run successful!"
```
