# Git pre-commit hook entry point.
# Checks are temporarily disabled; restore fog/grid smoke logic when ready.
# See git history for the previous implementation: git show HEAD:.tools/pre-commit.ps1
$ErrorActionPreference = "Stop"
exit 0
